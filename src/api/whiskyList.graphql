query WhiskyList {
    whiskyList(pageSize: 10000) {
        list {
            id
            name
            description
            countryId
            country
            regionId
            region
            age
            distilleryId
            distillery
        }
        totalCount
    }
}