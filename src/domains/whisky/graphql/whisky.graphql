query GetWhiskyById($id: String!) {
    getWhiskyById(id: $id) {
        id
        name
        description
        countryId
        country
        regionId
        region
        age
        distilleryId
        distillery
    }
}