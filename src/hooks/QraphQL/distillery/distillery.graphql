query GetDistillery($id: String!) {
    getDistillery(id: $id) {
        id
        name
        description
        countryId
        country
        regionId
        region
        children{
            id,
            name
        }
    }
}