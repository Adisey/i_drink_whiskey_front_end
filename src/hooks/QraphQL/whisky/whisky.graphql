query GetWhiskyId($id: String!) {
    getWhiskyId(id: $id) {
        id
        name
        description
        countryId
        country
        regionId
        region
        age
        distilleryId
        distillery
    }
}